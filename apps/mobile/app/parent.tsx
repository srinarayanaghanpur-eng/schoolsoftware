/**
 * LEGACY ROUTE FILE — superseded by app/parent/index.tsx (2026-07-21 rebuild).
 * Re-exports the new parent home so /parent renders the new UI regardless of
 * which duplicate route expo-router resolves. Delete this file during cleanup
 * (scripts/mobile-ui-teardown.ps1 removes it).
 */
export { default } from "./parent/index";
