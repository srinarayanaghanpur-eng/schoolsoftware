/** LEGACY ROUTE — old UI removed (2026-07-21 rebuild). Delete during cleanup. */
import { Redirect } from "expo-router";
export default function LegacyAdmin() { return <Redirect href={"/" as never} />; }
